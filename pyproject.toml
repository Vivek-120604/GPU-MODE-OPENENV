[project]
name = "gpu-mode-openenv"
version = "1.0.0"
description = "OpenEnv biological optimization agent"
requires-python = ">=3.10"
dependencies = ["openai>=1.0.0", "requests>=2.28.0", "aiohttp>=3.8.0"]
