[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "openenv-agent"
version = "0.1.0"
description = "OpenEnv biological optimization agent"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["openai", "requests", "aiohttp"]
